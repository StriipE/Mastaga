using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmericanNight : MonoBehaviour {

    public float topIntensity = 0.3f;

	
	// Update is called once per frame
	void Update () {
        
        transform.LookAt(Vector3.zero);
    }
}
